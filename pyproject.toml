[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fitszoom"
version = "0.0.1"
dependencies = [
    "numpy>=2.0",
    "astropy",
    "matplotlib",
    "pytest",
]
